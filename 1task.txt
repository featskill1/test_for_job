def is_even(num):
    if num & 1 == 0:
        return True
    else:
        return False

Первая функция лучше тем, что она короче написана и проще для понимания.
Моя функция быстродейственнее, т.к. на больших числах операция % может дольше обрабатываться
